# LUX-094：用户管理 API 实施记录

## 已完成

- [x] 管理员分页前的用户列表读取、创建用户。
- [x] 修改显示名、密码、管理员、服务器管理、远程访问和下载权限。
- [x] DELETE 语义为禁用用户，不删除历史数据。
- [x] 普通用户不能访问用户管理端点。
- [x] 数据库事务保护最后一个启用服务器管理账户。

## 约束

- 用户名保持不可变，避免令牌、审计和媒体状态关联断裂。
- 密码只接受明文输入到密码服务，数据库仍只保存 Argon2id 哈希。
- 管理权限修改实时影响新的认证解析；禁用用户会使现有 Web/Emby 认证解析失效。
