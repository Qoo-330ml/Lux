# ADR-005：本地元数据为权威

- 状态：已接受（需求确认）
- 日期：2026-08-02

## 决定

字段级元数据优先级为：锁定的管理员本地值 > 现有 NFO/本地图片 > 已确认 TMDb > 文件名、目录名和媒体探测 fallback。识别或编辑成功后，按原子写回规则更新 NFO 和选中图片。

## 原因

用户已有的整理结果应被保护；在线服务只负责补缺和提供候选，不应静默覆盖有效本地数据。

## 后果

媒体挂载需要可写，写回失败必须保留原文件并显式报告。NFO 读取要宽容、禁用外部实体，并尽量保留未知字段。
