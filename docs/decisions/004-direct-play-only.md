# ADR-004：首版只直放

- 状态：已接受（需求确认）
- 日期：2026-08-02

## 决定

首版只提供本地文件和 `.strm` 的直接播放，不实现音视频转码、容器转换、字幕转换或 HLS 转码。

## 原因

直放是个人 NAS 服务的主要路径，避免把首版复杂度和资源风险集中到转码系统。浏览器不支持的编码要清晰提示用户使用兼容客户端。

## 后果

PlaybackInfo 只声明真实支持的 DirectPlay 能力；任何转码需求都必须先修改规格并新增 ADR，不能伪造客户端能力。
